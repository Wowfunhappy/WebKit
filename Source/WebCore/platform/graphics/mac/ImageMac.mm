// MAVERICKS_BACKPORT: placeholder translation unit. PlatformMac.cmake lists this source, but its upstream BitmapImage/ImageAdapter Cocoa drawing helpers are not provided here for the 10.9 build, so the file compiles to an empty object to satisfy the source list.
#include "config.h"
