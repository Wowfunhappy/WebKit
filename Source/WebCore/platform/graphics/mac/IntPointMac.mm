// MAVERICKS_BACKPORT: placeholder translation unit. PlatformMac.cmake lists this source, but its upstream IntPoint<->NSPoint conversions are not needed on 10.9, so the file compiles to an empty object to satisfy the source list.
#include "config.h"
