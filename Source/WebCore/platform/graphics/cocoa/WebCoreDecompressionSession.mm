// MAVERICKS_BACKPORT: entire WebCoreDecompressionSession translation unit is gutted to an empty stub on 10.9 — its VideoToolbox decompression-session rendering path relies on SPI absent from this OS.
// Stubbed for 10.9
#include "config.h"
