// MAVERICKS_BACKPORT: empty stub translation unit kept in-tree as the 10.9 alternative to AVTrackPrivateAVFObjCImpl.mm, whose audio/video track-config extraction pulls in AVFoundation/CoreMedia format-description SPIs unavailable on 10.9. Not in the build's source list; the real .mm is built. Exists so the stubbed-out variant is preserved.
#import "config.h"
/* AVTrackPrivateAVFObjCImpl stubbed for macOS 10.9 */
