// MAVERICKS_BACKPORT: ScreenCaptureKit is macOS 12.3+; the entire upstream implementation is replaced by an empty translation unit on 10.9 (HAVE(SCREEN_CAPTURE_KIT) is off, screen/window capture unavailable).
// Stubbed for 10.9 - non-critical feature
#include "config.h"
