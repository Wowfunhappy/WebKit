// MAVERICKS_BACKPORT: ScreenCaptureKit / SCContentSharingPicker are macOS 12.3+; the entire upstream implementation is replaced by an empty translation unit on 10.9 (HAVE(SCREEN_CAPTURE_KIT) is off).
// Stubbed for 10.9
#include "config.h"
