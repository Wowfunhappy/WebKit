// MAVERICKS_BACKPORT: Mac video-presentation (fullscreen/PiP) interface gutted to an empty TU; VIDEO_PRESENTATION_MODE is off on 10.9 (AVKit PiP/in-window fullscreen unavailable), so the upstream implementation is replaced by this stub.
// Stubbed for 10.9
#include "config.h"
