// MAVERICKS_BACKPORT: Mac playback-session interface gutted to an empty TU; VIDEO_PRESENTATION_MODE / WEB_PLAYBACK_CONTROLS_MANAGER (Touch Bar / AVKit controls) are off on 10.9, so the upstream implementation is replaced by this stub.
// Stubbed for 10.9
#include "config.h"
