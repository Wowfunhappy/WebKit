// MAVERICKS_BACKPORT: empty stub standing in for the upstream WKBrowserEngineDefinitions header (BrowserEngineKit BE* types are iOS 17.4+ APIs); the 10.9 Mac build ships none of them, so includers just get an empty header.
#pragma once
