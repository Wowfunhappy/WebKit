// MAVERICKS_BACKPORT: empty translation unit so the source file PlatformMac.cmake lists in WebKit_SOURCES compiles on the 10.9 CMake build; the upstream XPCConnectionTerminationWatchdog implementation is not provided here (the watchdog reason string lives inline in AuxiliaryProcessProxyCocoa.mm).
#include "config.h"
