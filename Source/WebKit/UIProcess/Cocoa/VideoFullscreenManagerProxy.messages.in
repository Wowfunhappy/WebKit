# MAVERICKS_BACKPORT: VideoFullscreenManagerProxy needs ENABLE_VIDEO_PRESENTATION_MODE (off on 10.9) and is excluded from the Mac build, but the messages generator still references this name; this empty stub satisfies it without adding any real messages.
[
    DispatchedFrom=WebContent,
    DispatchedTo=UI
]
messages -> VideoFullscreenManagerProxy {
}
