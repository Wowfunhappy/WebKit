// MAVERICKS_BACKPORT: empty stub header. WKBrowserEngine definitions are
// iOS-only / BrowserEngineKit (iOS 16+) APIs the 10.9 Mac build does not ship;
// this placeholder satisfies `#include "WKBrowserEngineDefinitions.h"` from
// shared/UIProcess headers so the include resolves to a no-op on this build.
#pragma once
