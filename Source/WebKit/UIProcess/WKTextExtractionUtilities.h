// MAVERICKS_BACKPORT: empty stub header. The real WKTextExtractionUtilities
// lives in UIProcess/Cocoa/TextExtraction (text-extraction feature); this
// UIProcess placeholder satisfies `#include "WKTextExtractionUtilities.h"` from
// WebPageProxy/WKPreferences so the include resolves on the 10.9 Mac build.
#pragma once
