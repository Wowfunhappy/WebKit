--- a/gst-libs/gst/video/video-format.c
+++ b/gst-libs/gst/video/video-format.c
@@ -883,7 +883,7 @@
       U |= (U >> 10);
       Y |= (Y >> 10);
       V |= (V >> 10);
-      A |= (A >> 10);
+      A = (A >> 14) * 0x5555;
     }
 
     d[4 * i + 0] = A;
@@ -6004,7 +6004,7 @@
       B |= (B >> 10);
       G |= (G >> 10);
       R |= (R >> 10);
-      A |= (A >> 10);
+      A = (A >> 14) * 0x5555;
     }
 
     d[4 * i + 0] = A;
@@ -6064,7 +6064,7 @@
       R |= (R >> 10);
       G |= (G >> 10);
       B |= (B >> 10);
-      A |= (A >> 10);
+      A = (A >> 14) * 0x5555;
     }
 
     d[4 * i + 0] = A;
